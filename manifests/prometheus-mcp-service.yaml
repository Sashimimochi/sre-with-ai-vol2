apiVersion: v1
kind: Service
metadata:
  name: prometheus-mcp
  namespace: monitoring
  labels:
    app: prometheus-mcp
spec:
  type: ClusterIP
  selector:
    app: prometheus-mcp
  ports:
  - name: http
    port: 9000
    targetPort: http
    protocol: TCP
