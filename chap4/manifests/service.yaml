apiVersion: v1
kind: Service
metadata:
  name: prometheus-mcp
  namespace: mcp-servers
  labels:
    app: prometheus-mcp
spec:
  type: ClusterIP
  selector:
    app: prometheus-mcp
  ports:
  - name: http
    port: 8080
    targetPort: http
    protocol: TCP
